# Copy to .env (gitignored) and fill in. docker-compose reads this automatically.
# Google Maps Platform key for map tiles, English place search and photos.
# Enable "Maps JavaScript API" + "Places API (New)" in Google Cloud Console.
GOOGLE_MAPS_API_KEY=
